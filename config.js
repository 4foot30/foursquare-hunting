// Configuration (defined in module.exports so this object is returned by a
// node.js require call)
module.exports = {

    // Foursquare API access credentials
    foursquareAuth: {
        clientId: 'YOUR-CLIENT-ID-HERE',
        clientSecret: 'YOUR-CLIENT-SECRET-HERE',
    },

    // Google Geocoding
    googleAuth: {
        key: 'YOUR-KEY-HERE',
    }

};
